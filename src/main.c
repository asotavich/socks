#include "../inc/header.h"

const int WIDTH = 800, HEIGHT = 650;

int main(void) {
    prehistory(WIDTH, HEIGHT);
    //living_room(WIDTH, HEIGHT);
    //rules(WIDTH, HEIGHT);
    /*help(WIDTH, HEIGHT);
    help_bath(WIDTH, HEIGHT);
    help_kitchen(WIDTH, HEIGHT);
    help_living_room(WIDTH, HEIGHT);*/
    return 0;
}

